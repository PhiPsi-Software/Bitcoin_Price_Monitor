The following Python librarys needs to be installed:

pip install beautifulsoup
pip install colorama
pip install scipy
pip install requests
pip install helper
pip install simplejson
pip install pystray
